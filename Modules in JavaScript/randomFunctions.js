const add = (first, second) => {
    return first + second;
};

const multiply = (first, second) => {
    return first * second;
};

export {add, multiply}; // we can also export functions or classes like an object

const divideTheFirstNumberByTheSecondNumber = (first, second) => {
    return first / second;
};
export {divideTheFirstNumberByTheSecondNumber};
